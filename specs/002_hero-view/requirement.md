# Requirements Document

## Overview

This requirement will display a DSA hero (sample data can be found under `hero.yaml`) in a mobile
friendly way. As inspiration use the provided `Boronmir Siebenfeld von Ferdok.pdf` which is the
source for the `hero.yaml`.

