# The source of Tests/RulesEngineTests/Fixtures/pipeline-rules.json (make rules-engine-fixture).
# Fix round 1 (R37): action selectors, as STATE_8.H2 [any] and SA_65.VH1 [aktion].
id: pl-still
name: Starre
kind: specialAbility
source: { url: https://example.invalid/pl, book: Regelwerk, page: 1, checked: 2026-09-25, hash: null }
reviewed: null
clauses:
  - id: S1
    text: Keine Handlung.
    effects:
      - forbid: { what: { action: [any] } }
        when: { gmFact.frozen: true }
  - id: S2
    text: In der Haltung keine Aktion.
    effects:
      - forbid: { what: { action: [aktion] } }
        when: { choice.stance: true }
  - id: S3
    text: Kein Angriff namens Treffer.
    effects:
      - forbid: { what: { attack: [hit] } }
        when: { gmFact.hitCheck: true }
