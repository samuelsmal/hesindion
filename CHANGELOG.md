# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added

- **The resolver's own guards, because everything Task 10 built would have stayed silent if it broke** (task review, three Important findings, one theme). Three things had no way to fail. **(1) The closed-loop check** — "the ten golden rules resolve to exactly the URLs their authored files already record", the criterion that says the resolver *works* rather than merely runs — was a number in a report: no test, no target, no flag. And the obvious place to re-check it **cannot detect a failure**, because `build_db.import_effects` writes the authored `source.url` over the resolved one in the same column, so in `rules.db` the two can never disagree and the comparison passes unconditionally. The only comparison with anything to say is the resolver's own map against `specs/rules/*.yaml`, which is now two tests in `tests/rules/test_resolve.py`, skipped when no resolution is present, and the reason a `rules.db` version would be theatre is written into the file so nobody "simplifies" it into one. Verified to fire by rewriting one golden rule's map entry to the *extrapolated* spelling of its URL — the exact mistake the whole task exists to prevent — which fails with both strings named. **(2) `make rules-db-verify` could not see a `source_url` that never arrived.** `check_authored_fields` now checks it against the authored files, the same way and for the same reason `ruleset` is checked: a `source.url` that stops at the YAML leaves the authoring driver reading the Optolith seed, silently, which is the defect the calibration gate failed on. A file still carrying the `UNVERIFIED` placeholder is skipped, because the placeholder is deliberately never written and its absence is correct rather than missing. A new `check_resolution_reached_the_db` additionally requires every *confirmed* resolution to be in the column — except where an authored file legitimately overrode it, an exclusion that is not theoretical: the first version of that check reported a golden rule as stale for exactly that reason. A **missing** map is reported to stderr and not failed, because a fresh checkout has not run the resolver yet and the remedy is a command, not a repair. **(3) `import_resolved_urls` and the authored-wins / `UNVERIFIED`-skip merge had no tests at all** — `tests/rules/test_build_db_source_url.py` covers them: a confirmed resolution reaches the column, an authored URL overwrites it, the placeholder overwrites nothing and is never written even where there was nothing to overwrite, and a resolution for a rule the database lacks is counted rather than crashed

- **`UNVERIFIED_URL` and the resolved-map path are pinned across their call sites**, the way `NON_RULE_FILES` and `CHAPTER_PREFIX` already are. `UNVERIFIED_URL` arrived in `build_db.py` duplicated from `check.py` **without** the project's hand-sync convention — whole-branch review finding 5's defect class recurring in new code — so both sites now carry a comment naming the other and `tests/rules/test_shared_constants.py` asserts they agree; two spellings drifting apart would put `.../UNVERIFIED` into the database as a real URL for the driver to fetch. The resolved-map path is the same shape one level up: `resolve.py` writes it, `build_db.py` reads it into `rules.source_url`, `verify_db.py` reads it again to check that it got there, and the three are computed `parents[2]` expressions from three different depths. **What that assertion pins is a silent skip, not a typo.** They diverge when a file moves, and the consequence is not an error: `verify_db` treats a map it cannot find as "no resolve run has happened yet" — a legitimate state — so it prints a note, skips the check, and reports `rules.db is current` while every resolved URL goes unverified. Confirmed by construction: pointing one of the three one directory short leaves `check_resolution_reached_the_db` returning no errors with 201 resolved URLs unchecked

- **`make rules-resolve` (`scripts/rules_sync/resolve.py`) — every combat rule now has a page on the rule website, resolved from its name through the site's own category indexes.** Blocker 1's first half. The calibration gate's headline finding is that the driver feeds the agents the Optolith seed text while ADR-0007 makes the rule website normative; the reason it could not simply read the page instead is that 222 of the rules have no `source.url` at all. This is the deterministic half of ADR-0007's own reconciliation, with no model in it. It crawls the indexes for combat groups 3, 9, 10, 11 and 12 and takes each URL from an anchor text and an `href` the site publishes — **never extrapolated from a rule id**, which is not a stylistic preference: `KSF_Sturmangriff.html`, the percent-encoded `KSF_Vorsto%C3%9F.html`, a lowercase-hyphenated slug two directories deep and a page whose anchor text and file stem are *different words* all coexist in the corpus, and no rule about ids produces all four. Live run: **232 rules, 315 pages indexed across 6 index pages, 201 resolved, 26 needs-review, 5 unresolved, 0 ambiguous** — and the ten golden rules resolve to exactly the URLs their authored files already record, 10 of 10. Identity is confirmed per rule by the three signals Task 4 established by hand (page title, rule text against the seed text, `Publikation(en):` against the seed's `src:` block) and **all three must agree**; two of three is reported for a reviewer and the URL is not recorded, which is how the run independently reproduced Task 4's hand-found `SA_62` page discrepancy. A name matching no anchor or more than one is reported with the rule id, the name and the candidates considered. Fetching reuses `check.py`'s `Fetcher` — same disk cache, same 1 req/s, same User-Agent — and a second run makes **zero** network calls and prints the count. **What it does not do: author provenance.** A `source.hash` is a claim that a specific page was fetched and normalised, which ADR-0007's second 2026-09-21 amendment gives to the driver; this resolver produces the resolution and the report, and the 17 `UNVERIFIED` authored files are deliberately left alone

- **Five things the live rule website does that the resolver had to learn, each from a failing run rather than from a guess.** They are recorded because the next person to parse this site will hit all five. (a) An index page's `#main` is usually **empty** with the link list outside it, so `normalise_html` raises `ContentContainerEmpty` on it *by design* (Task 5 fix round 4) — index pages are therefore parsed on their own path, from raw `a.ulSubMenu` anchors, and the normaliser is not "fixed" to accommodate them. But **one category does the opposite** and puts its links inside a non-empty `#main`, so the *anchors* and not the container are what says which kind of page this is; classifying by the container reported all 74 rules in that group unresolved. (b) Every page declares `<base href="https://dsa.ulisses-regelwiki.de/">` and writes its anchors from the site root, so a sub-category index that itself lives in a directory links to `<dir>/<page>.html` — joining those against the page's own URL doubles the directory. (c) An anchor text carries the Stufen ladder the rule's name does not (`… I-III`). (d) The publication heading has four spellings (`Publikation:`, `Publikationen:`, `Publikation(en):`, `Publikationen(en):`); its entries are separated by a comma, a semicolon or nothing at all; and a rule printed across a page break is listed as `Seiten N - M`. Matching only the parenthesised heading read 28 real pages as having no publication line. (e) A book's volume numeral is roman in the seed and arabic on the site, and volume one carries no numeral at all — 55 of the first run's 96 `book not listed` reports were that and nothing else

- **`rules.db` carries each rule's `source_url`, which is where the name → URL map lives** (Data Policy: it is a generated, untracked build artifact, so no ability name enters git). `rules` gains a `source_url` column; `build_db.py` fills it from `make rules-resolve`'s **confirmed** resolutions, handed over through a JSON file under the git-ignored `.cache/`, and then from the authored files' own `source.url`, which wins — a URL a person reviewed and committed outranks one a crawl resolved, and the `UNVERIFIED` placeholder is deliberately not carried, since writing it would replace a resolved URL with a marker meaning "nobody has looked". 203 rows are populated after `make rules-resolve && make rules-db`. `make rules-db-verify` still reports `rules.db is current`: the hand-off file is read on both sides of the rebuild comparison, so the build stays deterministic and offline

- **`tests/rules/test_workspace_leaks.py` — the sanitised workspace is now checked for a withheld rule's *encoding*, not only for its name.** `prepare_workspace` withholds each run's authored files and redacts every surviving mention of them by id and by German ability name; `test_no_withheld_id_or_name_survives_anywhere_in_the_workspace` greps for exactly those two tokens, which is why it passed over ADR prose that stated four of the ten golden rules' graded rows outright (see *Fixed*). The new guard builds the workspace with **every** authored rule withheld and reads it back for four shapes an encoding has actually been restated in on this branch: a row spelled in schema field names, a tier ladder given as tier-and-value pairs, a graded value sitting beside its target in a passage that still points at a withheld rule, and a registered token that belongs to exactly one rule appearing in a narrative file. `specs/rules/schema.json` and `specs/rules/vocabulary.yaml` are exempt from the last of those, because glossing a token beside its meaning is their job and Task 6 fix round 2 ruled those glosses stay. Tier 1's field list is imported from `test_calibration.TIER1_FIELDS` rather than copied, so the guard cannot drift from the gate it stands in for. **It is a ratchet, not a proof:** a leak paraphrased into prose that shares no tokens with the encoding defeats every probe, and the module docstring says so rather than implying coverage it does not have

- **`docs/rules-pipeline-status.md` — the operational state of the rules pipeline, tracked.** The branch's rulings, findings and corrections lived in a git-ignored session ledger that is deleted when the branch finishes, so everything load-bearing had to become readable from the repository alone. Decisions belong in ADRs and tasks belong in the plan; what was left over is neither — a gate's result and what composes it, a measurement, an open ruling, a deferred minor — and it had no home. The document carries: the calibration gate's **7 of 10 and why that is the best of four runs rather than the expectation** (pooled 23/40 ≈ 0.58, 95% interval roughly [0.40, 0.89]), the two-tier grading rubric with the reason **byte equality with the golden corpus is now evidence of contamination rather than success**, `SA_43`'s variance datum (the simplest rule in the corpus dropped its `when: [{mounted: true}]` gate in 2 of 5 passes on identical input — ADR-0008's named failure mode, invisible to both the linter and the author/verifier cross-check), the **five blockers** on any 222-rule wave, the two live app bugs this branch found and fixed with the method that found them, the open questions awaiting a ruling, the deferred minors, and the corrections that would otherwise be re-derived the hard way. `AGENTS.md` points at it, because it is the file that says *do not start a wave*
- **An unstated rule set is now marked, not guessed silently** — `UNCLEAR-RULESET:`, a root-note prefix greppable exactly like `UNENCODED:` and `DISAGREEMENT:`. ADR-0009 tells both authoring agents to write `core` when the page does not say which set a rule belongs to, and the reasoning stands: wrongly-`core` fires for a table that did not choose it and somebody sees a number that should not be there, while wrongly-`focus.` fires for nobody and nothing says so. But guessing in the visible direction only makes the *rule* visible, not the guess — and the rationale that recorded the doubt lives in a review file under the git-ignored `.proposals/`, so it is gone once the rule is committed. The marker puts the doubt in the corpus: `grep -r UNCLEAR-RULESET specs/rules` enumerates every rule whose set was guessed rather than read, the way `UNENCODED:` enumerates the encoding debt. Stated in `schema.json`'s `note` description, in `AGENTS.md`, and in both agent briefs (`.claude/agents/rule-author.md`, `rule-verifier.md`)
- **`ruleset` and `source.title` reach `rules.db`, which is where the engine reads** (ADR-0009, the half that was deferred). `rules` gains a `ruleset` column and a `title` column; `scripts/build_rules_db/build_db.py` populates both from the authored files. This was originally listed as a consequence to be picked up by the engine plan, which was the wrong place for it: the engine reads the database, not `specs/rules/`, so a `ruleset` declared, linted, glossed and backfilled across 28 files still left every rule applying to every hero. A `rules` row with no authored file keeps `ruleset` NULL — correct rather than missing, since it has no authored encoding and so no effects to filter. `make rules-db-verify` now checks both columns **against the authored files**, not against another copy of the build's own output: the dump comparison it was built on is blind to this class of bug, because a field the build drops is dropped identically on both sides and two NULL columns compare equal. What is still the engine plan's work is the filtering itself
- **`source.title` on chapter rules, with a Data Policy guard that is a fold rather than a length cap.** A `CHAP_` rule has no `rules_i18n` row — there is no Optolith entry behind it — so a breakdown line citing one would render its id and nothing else, which ADR-0009 turns from a curiosity into a blocker once the UI has to explain where a number comes from. `specs/rules/CHAP_Reiterkampf.yaml` now carries `title: Reiterkampf`, the schema accepts the field, and `build_db.py` writes it into the chapter rule's `rules` row. It is the one field in a chapter file that holds German, so `scripts/rules_lint/lint.py` requires it to **ASCII-fold to exactly the slug the id derives from `source.url`** — ADR-0009's own argument for admitting the field ("the id *is* the page title, ASCII-folded") made mechanical. A description of the page folds to something that is not the slug and is rejected; only the display spelling of a name the repository already holds can pass
- **Every authored rule declares its `ruleset`, and every engine output will carry the rule id it came from** (ADR-0009, the data half). Two decisions from one cause: a number the app shows cannot say where it came from. `ModifierLine` carries a value and a *localised label*, so a `-1 BE` line in a combat breakdown cannot name the Reiterkampf chapter or link its page — and the session's three findings (a `-2 x level` Belastungsgewöhnung formula the authored `SA_41` already contradicted, a mounted `-2 AW` with no attribution anywhere, two legacy sources disagreeing about `SA_41`'s scope) were each invisible until somebody went looking. The decision is that every `EngineResult` output names its rule, and the UI explains any number it shows by rendering that rule's **own name and text, in German, straight from `rules_i18n`** — no English display text, no translation layer, and no relaxation of the Data Policy: rule prose still never enters git and still reaches the app only through the generated database. The Swift change to `ModifierLine` belongs to the engine rewrite (ADR-0008); this changelog entry is the data side
- `ruleset` in `specs/rules/schema.json`, enforced by `make rules-lint`, backfilled across all 28 authored files: `core` (a standard rule of the Regelwerk), `focus.<slug>` (a Fokus-Regel) or `house.<slug>` (a table's own rule). **Required, with no default** — `core` is the common case, which is exactly why defaulting to it is wrong: an unmarked Fokus-Regel would apply to every hero and the file would still look correct. A `focus.`/`house.` slug must be glossed in `specs/rules/vocabulary.yaml`, the same registration `gmFlag` and the `actionEconomy` tokens already carry, so `focus.trefferZonen` beside `focus.trefferzonen` fails the lint instead of becoming one optional rule that appears as two. All 28 current files are `core`; `focus.trefferzonen` is registered and unused, because the hit-zone rules are still Swift (`HitZoneModifiers`, `HitZoneTable`, `WoundEffectCatalog`) and have no authored file to land in yet. The engine applies only the sets a hero plays with, so `FokusRule` becomes a projection of that registry rather than a hand-maintained Swift enum, and `Hero.fokusRules` needs no data conversion — the slug is spelled to match the existing enum's raw value
- **Versioning stays out, deliberately** (ADR-0009): one current text per rule, with `book`/`page`/`checked`/`hash` recording which printing it was verified against and `make rules-sync-check` catching a change. The worked example of what is deferred is `SA_62`, where the Optolith seed says page 249 and the live page says 250 — ADR-0007 makes the page normative, so the file records 250 and the seed is stale, where a versioning scheme would instead record two printings and which one a table plays under. Acceptable because the app serves one table on one current printing, the drift checker already turns a silent change into a dated report, and a version dimension would extend `ruleset` plus the `source` block rather than replace them
- **Rule identity is ours; Optolith names abilities only** (ADR-0009). `CHAP_<PageSlug>` is reframed from "a second namespace beside Optolith's" to the first instance of our own, with Optolith demoted to the id source for abilities. Two more categories belong in it and are still Swift literals: the named combat constants ADR-0008 turns into parameters (the multiple-defence `-3`, the reach matrix, the zone penalties, the dual-wield and Passierschlag bases), and the derived-value rules — GS and the species base LP/SK/ZK, which ADR-0006 already had to correct once. Task 12 of `docs/plans/2026-09-20-rules-pipeline-and-authoring.md` now enumerates both with the file and line each occupies. Nothing is authored by this change
- **A chapter rule has no name to display, which is now a blocker rather than a curiosity.** ADR-0008's amendment noted that a `CHAP_` id has no `rules_i18n` row; with a UI that cites rules, a breakdown line for `CHAP_Reiterkampf` would show an id and nothing else. ADR-0009 decides that a chapter file carries its page title in `source.title` and that `build_db.py` writes it into the chapter rule's `rules` row — rejecting the alternative of fetching the title at build time, which would put a network dependency back into a build ADR-0007 requires to be offline. The title is not new information in git: the id *is* the page title, ASCII-folded, and `lint.py` already enforces that. The field lands with the first chapter file that needs it (Task 12), not now
- Both agent briefs gained hard rule 11: a proposal carries its `ruleset`, decided from how the rule is *printed* rather than from how its mechanics look, and **when the page does not make it obvious the agent says so in its rationale and writes `core`** — not because `core` is likely, but because the two ways of being wrong are not symmetric. A rule wrongly marked `core` fires for a table that did not choose it and somebody sees a number that should not be there; one wrongly marked `focus.` fires for nobody and nothing says so, which is the silence ADR-0008 exists to prevent. `ruleset` also joins `subgroup` and `excludes` in `propose.py`'s scalar diff, because two readings differing about it decide whether the rule fires for everybody or for nobody. The driver does not default it the way it defaults `subgroup`: `subgroup_id` is a database column, "is this an optional rule" is a judgement about the page
- The ten golden files gained `ruleset: core` and their `tests/rules/golden/MANIFEST.yaml` hashes were updated **without re-running the calibration gate**. `ruleset` is a rule-root key and Tier 1 grades effect rows against `TIER1_FIELDS`, which does not list it, so no byte the backfill touched is an input to any of the ten recorded verdicts; the argument is recorded under `golden_edits` in `tests/rules/calibration/2026-09-21-sonnet/RUN.yaml` and pinned by `test_ruleset_is_outside_what_tier_1_grades`, which fails if `ruleset` is ever added to `TIER1_FIELDS`. The recorded proposals in that directory are deliberately left without the field — they are agent output from a run that predates it, and writing it in would falsify the measurement — so they are linted with a one-key `allow_missing_ruleset` waiver. The next recorded run gets no waiver
- **The authored corpus covers chapter rules, not only rules with an Optolith id** (`CHAP_` id namespace; ADR-0007 and ADR-0008 amended). A DSA 5 chapter page states mechanics that bind anyone in the situation it describes, with no ability to hang them on, so Optolith has no id for them and the corpus had nowhere to put them — while the app hardcoded their constants in Swift, which is exactly what ADR-0008 says should be data. A chapter id is `CHAP_` plus its page's own URL stem (`CHAP_Reiterkampf`): disjoint from the Optolith namespace by construction, since every Optolith id ends in digits and every chapter id ends in letters, and derived from `source.url` rather than from a registry that could drift. `scripts/rules_lint/lint.py` enforces the derivation, that the page is real (the `UNVERIFIED` placeholder cannot stand in for a chapter id, which *is* its page) and that `subgroup` is `none`; `scripts/build_rules_db/build_db.py` creates the `rules` row itself under a new `chapter` category, since without it a chapter rule's effects would be skipped with a warning
- `specs/rules/CHAP_Reiterkampf.yaml` — the first chapter page authored, against <https://dsa.ulisses-regelwiki.de/Reiterkampf.html> with a verified `source.hash`. Three mechanics the app has implemented in Swift since before the corpus existed are now data: the mounted BE relief (`Hesindion/Engine/SharedModifiers.swift:15`), the mounted `-2` on Ausweichen (`Hesindion/Engine/DefenseModifiers.swift:47-53`) and the `+2` AT for the rider's advantageous position against a foot opponent (`Hesindion/Engine/MeleeModifiers.swift:11-17`, rendered by `CombatAttackViews.swift:404-443`). Its other clauses — the initiative base, the mount-control probes, the fall check and its damage-scaled escalation, the Passierschlag targeting restriction, the maneuver and equipment bars, the three mounted commands — are `reminder` rows with `UNENCODED:` notes, because a chapter rule has no `rules_i18n` text to degrade to and silence is the failure mode ADR-0008 exists to prevent. No Swift was changed
- `CHAP_Reiterkampf`'s BE row is `scope: combat`, and the Swift now matches it. The clause scopes the mounted relief to Kampfproben; the ruling that settled it is under *Fixed* below, and the row's note records the date rather than an `OPEN:` marker. This was the same seam as ADR-0008's last consequence about `CheckDomain` having no domain for INI or GS, in the other direction — that one is still open
- `tests/rules/test_calibration.py` + `tests/rules/calibration/` — the calibration gate. A live run of the pipeline over the ten golden ids is recorded once and graded here on every `pytest`, so the gate is reproducible without spending twenty model calls. Two tiers, fixed before the run: **Tier 1** compares every mechanical effect row on the fields that say what the rule does (`type`, `target`, `value`, `tier`, `when`, `side`, `recipient`, `parameter`/`set`/`shiftSteps`, `grants`/`forbids`, `add`, `action`, `scope`), unordered and with `note` excluded; **Tier 2** — reminder row versus `UNENCODED:` note, note wording, row order — is reported and never graded, because Task 6 established that byte equality with the golden files is the signature of contamination rather than success. **The gate does not pass: Tier 1 is 7 of 10.** The recorded proposals are linted here too, since `make rules-lint` globs `specs/rules/` only
- **The calibration gate does not certify a wave yet, and says so with a range.** 7 of 10 is the best of four runs of the same ten rules; pooled it is 23 of 40 (~0.58, 95% interval roughly [0.40, 0.89]). At that spread the gate cannot attribute a one-or-two-rule fix, cannot distinguish 6 from 8, and cannot certify what it exists to certify — measuring per-rule stability by repeating the same configuration is a blocker in its own right. The sharpest instance: the simplest rule in the corpus, a single `legality` row, dropped its `when: [mounted: true]` gate in 2 of 5 passes on identical input, which unlocks mounted-combat actions for a hero on foot — ADR-0008's named failure, invisible to the linter, and invisible to the two-agent cross-check when both agents drop it
- **The calibration gate's finding: the pipeline reads the wrong source.** The agents are handed `rules_i18n.description` from `rules.db`, which is the Optolith seed, while the golden ten were authored from the rule website that ADR-0007 makes normative — and on the rules where the two disagree the pipeline is confidently wrong. One golden rule's seed text states a damage bonus where the rule website states a defence bonus; another's is missing the Erschwernis clause and both cap clauses the rule website's page carries. Re-running the same driver, the same workspace and the same model against the rule website's text reproduces both golden encodings, one of them exactly. Both agent briefs told the agents *"the text you are given is the rule website's"*, which was never true. Until a rule's text comes from its `source.url`, a 222-rule wave will encode stale rules cleanly, and the two-agent cross-check cannot see it: both readers share the bad input. **ADR-0007 already decided this** — its Decision demotes Optolith to a seed "authoritative for neither text nor mechanics" after bootstrap and specifies reconciliation as fetch-and-normalise, then propose; the driver substituted the seed for the first step's output. It is necessary and **not demonstrated sufficient**: a ten-rule pass on rule-website text scored 6/10, below the seed run
- The rule's tier count (`rules.levels`) now reaches both agents as a `tiers: N` line in the input block, and both briefs state that it is authoritative for a ladder's extent and that each tier's rows carry that tier's *running total*, not its increment. The rule text usually states only a rate ("per Stufe"), leaving the ladder's extent to the rule website's page title and cost line, neither of which survives into `rules.db` — so the first calibration run encoded Stufe I alone for both laddered rules in the golden ten, correctly saying in its rationale that the text did not state how far the ladder ran. The driver had the number the whole time and did not pass it
- Both briefs gained two more encoding shapes that calibration caught: a clause suppressing or replacing a **named DSA constant** is a `parameterOverride` even when the constant belongs to the opponent's equipment (hard rule 5 is about outcomes and states, not numbers), and a `dice` row that only **redirects** damage carries no `add` at all rather than `add: 0`. Each fix moved exactly one rule from mismatch to match, measured live
- `make rules-propose` (`scripts/rules_sync/propose.py`) plus `.claude/agents/rule-author.md` and `.claude/agents/rule-verifier.md` — the authoring half of the rules pipeline. Two agents encode a rule independently from its text and the driver diffs them: the author takes a batch of up to 12 rules, the verifier takes **one rule's text only and never the author's YAML**, and where the two readings disagree the review file says so in its first section and the written YAML carries a `DISAGREEMENT:` prefix on its root note — greppable the way `UNENCODED:` is, and never silently resolved. The driver owns everything deterministic (batching, selectors `--ids`/`--group`/`--subgroup`, provenance, linting, diffing, the summary table) and delegates exactly one operation to a pluggable `Runner`; the shipped `SubprocessRunner` drives headless `claude -p`, and the injectable `FakeRunner` means the test suite makes zero model calls and zero network calls. Nothing is committed by the script
- Provenance stays with the deterministic half: an agent never emits a `source:` block (a guessed URL or hash is worse than none, and one that appears anyway is discarded and reported). A proposal carries its existing file's provenance forward, or the `unverified` placeholder `scripts/rules_sync/check.py` already reports; `--verify-source` re-fetches through `check.Fetcher` and re-hashes with `normalise.hash_html`, and a failed fetch or a `ContentContainerError` carries the old values forward rather than fabricating new ones
- Agents run against a **sanitised workspace** holding the schema, the vocabulary registry, the ADRs and the rest of the corpus, but not the authored file for any rule in the run — and with every remaining *mention* of those rules — **by German ability name as well as by id** — redacted to `a withheld rule`, any parenthetical that named one dropped outright. Found end-to-end rather than in theory, twice: given the repo as their working directory both agents read `specs/rules/SA_65.yaml` and reproduced the hand-authored file down to the wording of every note; and withholding the files alone still left four of the golden ten glossed by id in `vocabulary.yaml`, a fifth named in `schema.json` and a sixth's mechanics restated in ADR-0008 from a paragraph that is now stale. Redacting the id alone left the worse half standing: the same sentences named the ability in German while stating its exact encoding — which field a `dice` row uses, which rule another one excludes — and a token-shaped grep could not see it. To a model that has read the rules a German name identifies it as precisely as `SA_59` does. Both agents have `Grep`, so a hint either can find pushes them toward agreeing with each other rather than toward being right. What a workspace cannot reach — the agent briefs, which arrive as a system prompt — is covered by `ids_named_in_agent_briefs`, and the briefs' worked example is now a synthetic rule whose id belongs to no corpus
- The rule text is fenced and declared untrusted data in both prompts and both briefs. The two-agent design detects *independent* error; the agents share an input, so an instruction injected into a rule's text steers both identically and the run would report `agree / ok / written`. Paired with `parse_agent_output` keeping the first envelope per id, so an injected marker cannot re-open one
- `effects` now requires at least one row (`schema.json`), and the driver refuses an empty encoding before the verdict is computed: two agents that both return nothing used to agree, lint clean and be written, so a systematic authoring failure across 222 rules would have reported as a clean run
- `make rules-lint` rejects an unresolved `DISAGREEMENT:` marker, so a proposal cannot be committed until someone has decided which of the two readings is right; the driver is the one caller that may write it. The marker is also short now, with the summary in the review file — the first version overflowed the schema's 200-character note cap and silently erased the author's `UNENCODED:` prefix, breaking the invariant that `grep -r UNENCODED specs/rules` enumerates the whole encoding debt
- `--force` is required to overwrite a golden-corpus rule in `specs/rules/`, which a bare `make rules-propose RULES=SA_65` otherwise did to the very file the pipeline is calibrated against
- `.proposals/` is git-ignored and every review file opens saying so: the rationales quote the rule's clauses, which is what makes them reviewable and what keeps them out of git (Data Policy). The written YAML gets a second guard beyond the linter — any five consecutive words of the rule text reappearing in an encoding aborts that rule unwritten, which covers the free-form string fields `AGENTS.md` names as the linter's known blind spot
- `specs/rules/SA_62.yaml` (Sturmangriff) — the ability whose absence produced ADR-0008, now authored: `runUp ≥ 4` and `GS ≥ 4` preconditions, `dice: 2 + ceil(self.gs / 2)` (ceil per ADR-0006 — the rule says *half GS*, not *per full 2*), `actionEconomy: opponentPassierschlagOnFailure`, the page's flat −2 Erschwernis, a `reminder` for the defensibility clause, and `excludes: [SA_48]` for the combination ban the rule states
- `specs/rules/vocabulary.yaml` — the open-vocabulary registry. ADR-0008 closes the effect types and the condition predicates but deliberately leaves `actionEconomy.grants`/`forbids`, `legality.action` and the `gmFlag` slug open, because they are not finite over 232 rules and an enum would make every novel ability a schema PR. Open is no longer unrecorded: every token needs a one-line English gloss here, `make rules-lint` rejects any that lacks one, and `schema.json` now constrains all four to a camelCase slug (`legality.action` also allows `+`-joined segments, since its own worked example is `basismanoever+spezialmanoever`). Together these close a real Data Policy hole — a `forbids` value spelled as a whole German clause from a rule page previously linted clean, and the `text`-key and comment scans cannot see inside a free-form string field. The registry becomes the enum once the corpus stops growing
- `tests/rules/golden/MANIFEST.yaml` + `tests/rules/test_golden.py` — the ten hand-authored, fully-provenanced rules are frozen by content hash rather than duplicated. An edit to any of them fails with the remedy named ("golden corpus drifted — re-run calibration"), and the second copy that could have drifted from `specs/rules/` — and was the one place in the repo where a `text:` key or a German comment would have passed CI, since `make rules-lint` globs `specs/rules/` only — no longer exists
- A `note` convention on authored rules, stated in `schema.json` and applied across the corpus: identify a clause by position and mechanism, never restate its content (a `reminder` effect has no field but `note`, so per-clause reminders are exactly where translation pressure lands). Clauses the grammar cannot express are prefixed `UNENCODED:`, so `grep -r UNENCODED specs/rules` enumerates the whole encoding debt in one command
- `make rules-sync-check` (`scripts/rules_sync/check.py`) — deterministic, model-free drift detection for `specs/rules/*.yaml`: normalises the rule text a `source.url` currently serves (`scripts/rules_sync/normalise.py`: BeautifulSoup extraction scoped to `<main>`, `<br>` → newline, NBSP → space, whitespace-run collapse, NFC), hashes it, and compares against the recorded `source.hash`. Reports each rule as `ok`, `drifted`, or `unverified` (the `url: .../UNVERIFIED` placeholder every migrated rule currently carries) and exits non-zero only when something drifted. Requests are cached on disk under `.cache/rules_sync/` (git-ignored) and rate-limited to one per second, reusing the `DELAY`/`HEADERS` convention from `scripts/scrape_effects/scrape_effects.py` — a warm cache makes zero network calls
- `specs/rules/schema.json` — the authored-rule schema (draft 2020-12): the contract for one YAML file per DSA rule, closing the nine ADR-0008 effect types and ten condition predicates as enums, requiring `source.hash` provenance, and forbidding a `text` key (recursively, plus a comment scan) so rule prose stays out of git (Data Policy). `modifier` carries `target`/`scope`/`side`, `dice` carries `recipient`, `actionEconomy` carries `forbids` alongside `grants`, and `recovery` is now a ninth effect type — all per ADR-0008's fix-round-1 amendment closing the contract gaps a review found against the live 79-row effect corpus. `make rules-lint` (`scripts/rules_lint/lint.py`) validates every `specs/rules/*.yaml` file against it and exits non-zero on any violation
- `make rules-db` / `make rules-db-verify` — `Hesindion/Resources/rules.db` is now a generated, untracked build artifact rebuilt from the local Optolith source data (`RULES_SOURCE`), whose checksums are pinned in `specs/rules/SOURCES.yaml` and checked before every build (`scripts/build_rules_db/check_sources.py`)
- Trefferzonen (DSA 5 Fokus-Regeln) — optional hit-zone rules, off by default and switchable per hero. Attacking: a zone picker feeds the Zonenaufschlag (Kopf −10, Torso −4, Gliedmaßen −8, halved by Gezielter Angriff/Schuss, eased by 2 against a surprised target) into the attack roll, and a read-only card states the zone's wound effect for the GM. Taking damage: the zone is tapped or rolled on 1W20, damage is compared against the Wundschwelle, and a failed Selbstbeherrschung check applies Betäubung (Kopf), Liegend (Beine) or an extra 1W3+1 SP (Torso). All ten published zone tables are implemented — humanoid, vierbeinig, sechsbeinig mit Schwanz, Fangarme, and creatures without distinct zones
- Per-rule Fokus-Regeln toggles in the hero settings screen — the optional rules are a table's house rules, so they are a per-hero setting that persists across combats, and a group can adopt individual rules rather than all or nothing
- The Optolith `raceId` is now persisted as `PersonalData.speciesId`, making species-dependent derived values recomputable in future
- `HesindionUITests` XCUITest target — the repo's first end-to-end UI tests. Four tests drive the Trefferzonen surfaces on the simulator and attach screenshots (`docs/screenshots/01-fokus-settings.png` … `04-wound-effect-panel.png`)
- `make screenshots` — runs the UI tests on the single named simulator and exports the attachments to `docs/screenshots/` via `xcresulttool export attachments` (`scripts/export_screenshots.py` renames the exports to the attachment names)
- Debug-only `-uitest-seed-hero` launch argument — seeds a throwaway store with the bundled sample hero, the Trefferzonen Fokus-Regel on and an open combat session, so UI tests start from the same state every run. Compiled out of Release builds and inert without the argument

- Zustände & Status tracking per hero — a static catalog of 8 DSA 5 Zustände (leveled I–IV) and 17 binary Status with localized effects, cause, and removal rules; managed via a "Zustände & Status" section on hero detail (swipe-to-remove rows, add picker, detail sheet with prominent removal rules) and a shared `StatesStrip` of chips in combat
- Automatic modifier integration for states: active Zustände feed penalties into the ModifierEngine with the DSA −5 Zustand-penalty cap; combat root shows a states strip, a Handlungsunfähig/Bewegungsunfähig warning banner, and per-round reminders
- Entrückung "gottgefällig" toggle in spell and liturgy casting
- Command palette (Cmd+K) entries for states — "Zustand: …" / "Status: …" set a level (0 removes), covering add/level/remove from the keyboard
- Drag-to-reveal (swipe) removal of states in hero detail, matching the app's `SwipeActionRow` edit style; the reusable `SwipeActionRow` was extracted to its own file
- Schip (fate point) reroll option on failed skill and spell checks — on a regular failure, the locked 3W20 dice become reroll-selectable (all selected by default) and a "Schip: Neuer Wurf" button spends one Schip to reroll the chosen dice
- UI snapshot testing infrastructure using swift-snapshot-testing
- Snapshot tests for 7 views (HeroList, HeroDetail, Combat, CombatRoot, Adventure, DiceRoll, WeatherDay) across 12 iPad/color/dynamic-type variants
- `make test-ui` and `make test-ui-record` Makefile targets
- TestData factory for creating fake SwiftData models in tests
- `DiceRoller` engine — pure, testable dice rolling with an injectable `RandomNumberGenerator`
- Statistical tests for dice fairness (`DiceRollerTests`): chi-square uniformity for W3/W6/W20, mean, serial-independence, and end-to-end 3d20 → SkillCheckEngine critical-rate checks
- Success-probability tests verifying sampled pass rates for realistic ability profiles match exact enumeration of all 8000 outcomes
- Per-ability **theoretical success rate** on each talent row — traffic-light dot + % computed by exact enumeration (`SkillCheckEngine.successProbability`)
- Per-ability **recorded success rate** (overall %, number of Proben, sessions, and best session), revealed under every talent row at once via the Talents section's **"Aufgezeichnete Werte"** toggle
- **Session grouping** of the action log into play sessions separated by ≥ 8h gaps (`SessionGrouper`), with per-session success-rate headers in the Log panel
- `TalentStatistics` engine for aggregating recorded checks, plus tests (`SessionGrouperTests`, `TalentStatisticsTests`, `SuccessProbabilityTests`)
- `specs/rules/*.yaml` — the 26 hand-authored rule files (89 effect rows: the 79-row legacy migration plus 10 rows added in fix round 1 — restored tier ladders for `SA_41`/`ADV_25`/`ADV_44` and a `reminder` for `COND_4`'s Stufe IV) migrated from the legacy `specs/data/rules.yaml`/`HARDCODED_EFFECTS` stores per the ten-type mapping in ADR-0007/ADR-0008, one file per rule, all passing `make rules-lint`
- An eleventh `when` predicate, `gmFlag(<slug>)` — a GM-adjudicated condition (e.g. "at a known location", "principles violated") for the eight legacy free-text conditions with no mechanical predicate, surfaced as a GM toggle rather than silently becoming unconditional
- `effects.payload` column in `rules.db` — the authored effect stored as JSON verbatim, so a schema field added later costs no DB migration
- `note` key in `specs/rules/schema.json` (rule root and effect level; ASCII-only, length-capped) — an English annotation of which clause/mechanic a non-obvious authored encoding carries, since `lint.py` bans every `#` comment and the corpus otherwise carries no trace of what an effect means
- `docs/rules-migration-reconciliation.md` — the tracked reconciliation report tying all 79 legacy `specs/data/rules.yaml` rows to their authored `specs/rules/*.yaml` effect (plus the 10 rows added in fix round 1), so the migration's evidence survives independent of `.superpowers/` (gitignored)

- **`scripts/rules_sync/rule_graph.py` — the sanitised workspace's withholding now follows the rule graph, not just the rule id** (whole-branch review §6, the controller ruling recorded after the blocker fixes; prerequisite for the Task 11 stability measurement). Closing finding 1 surfaced a leak channel no redaction guard can reach: `specs/rules/CHAP_Reiterkampf.yaml` is a legitimate, un-withheld authored file that encodes the very clause `SA_661` modifies — same `type`, `target`, `scope`, `side`, value and gate — and it carries neither `SA_661` nor its German ability name, so withholding the graded file and redacting its two tokens left the answer standing in a file the agent was entitled to read. `prepare_workspace` now additionally withholds the **transitive closure** of the run's rules over three edges computed from the corpus as it is: (1) `excludes`, walked **undirected**, because a rule that names the graded one still states a Tier 2 field of it and the graded rule has no field of its own to give that away; (2) a shared `parameterOverride.parameter` path, where one rule states what the other's constant is; (3) **graded-axis collision** — a non-reminder effect row of each agreeing on every axis Tier 1 grades a row's shape by (`type`, `target`, `scope`, `side`), which is the edge that catches `CHAP_Reiterkampf` ↔ `SA_661` and the reason a declared-relationship field would not have been enough. There is deliberately **no new `related:`/`modifies:` schema field**: the adjacency that leaked is precisely the kind nobody spots, so an edge depending on an author spotting it inherits the defect it is meant to remove. A row declaring none of `target`/`scope`/`side` has no axis to collide on and does not fire edge 3 — without that boundary the degenerate all-absent key would make every `parameterOverride` row adjacent to every other and swallow edge 2 whole, which is specified separately precisely because parameter identity rather than row shape is what makes two overrides neighbours. Withheld and redacted stay two different sets: a neighbour is withheld as a *file*, and is not added to the redaction token set, because redaction's cost is that a registry gloss loses its pointer (Task 6 fix round 2 ruled those glosses stay) and a neighbour's German name is not an input here, so it could only ever be a half-redaction of the kind that round was opened to fix

- **The run writes `WITHHELD.md` beside its other artefacts** — §6's second requirement, and the thing that makes the closure auditable: per graded rule, its own file plus every additionally-withheld file with the edge that pulled it in, the axes it collided on, and how many hops from the graded rule it sits. A rule with an empty neighbour set says so in words, because silence must not be readable as absence: a later reader has to be able to tell "this rule had no authored neighbour" from "the graph missed the neighbour it had". It lands with the run's artefacts and **not** in the workspace — the list of withheld files is exactly the hint the workspace README refuses to give — and it carries ids, file names, field names, axis values and edge kinds only (Data Policy), pinned by a test that greps it for every `note` in the corpus. The workspace README now says the precedent is thinner than the corpus is and why, without naming a file

- **Two more edges, each added after the first three were measured against the corpus and found to leave a live channel standing** (Task 11a fix round 1). (a) **`scope` collides by subsumption, not only by equality.** `scope` is a domain filter — `all` means every domain, `combat` the combat domains — so a `scope: all` row mechanically subsumes a `scope: combat` row on the same target. The corpus's only two `modifier target: be` rows are exactly that pair, on `SA_41` and `CHAP_Reiterkampf`, and under exact equality the chapter file stayed in `SA_41`'s workspace with that row intact — in front of the one golden rule that has never passed in five runs, whose recorded failure is precisely that it invented a `parameterOverride` path where the golden has a `modifier target: be` row. That channel can only move it toward the golden. **`target` is deliberately not widened the same way**, and the reason is measured rather than asserted: wildcarding `target: all` links every modifier row to every other, taking nine of the ten golden rules to 21 withheld files and the batch to 22 of 28 (re-measured in fix round 2 against the graph as it ships; the first figure was taken mid-round, before the `when` edge landed) — the swallow-everything failure the axis-less boundary already rejects, arrived at from the other side. (b) **A fourth edge, `when`-predicate identity.** `SA_43`'s entire golden encoding is one `legality` row plus one `when` predicate: it declares no axis, overrides no parameter and names nothing in `excludes`, so the first three edges found it no neighbour at all — while a chapter file carrying that same gate on every one of its own rows, and naming `SA_43` in two of its notes so that redaction rewrites them and leaves the mechanical half standing, sat in its workspace untouched. A neighbour that states the *gate* is a fuller answer key for that rule than one that states the *shape*, and `SA_43` is the source of the 2-of-5 gate-dropping datum Task 11 exists to quantify. Rows with no `when` contribute no edge: "both unconditional" is not a shared gate, and treating it as one would swallow the corpus the same way

- **The withholding report shows every edge between a pair, not the first one found.** `SA_48`/`SA_62` are joined by both an `excludes` edge and a graded-axis collision, and the report used to name only the first — which reads as a Tier 2 adjacency and understates what the measurement controlled for. A `report_path` inside the workspace is now **refused** rather than merely discouraged: it names every withheld file, which is the whole hint the workspace README declines to give, and until now that was a docstring promise with nothing enforcing it. The README itself no longer enumerates the edges: by construction no surviving file shares a graded axis with a graded rule, so an agent reading that list literally could reject any candidate `(type, target, scope, side)` it found in the visible corpus — a filter over exactly the four fields Tier 1 grades. It now says the precedent is thinner, says not to reason from what is missing, and stops there

- **What the closure costs, measured before it is spent** (`docs/rules-pipeline-status.md` §2). Over the ten golden rules run as one batch — the recorded calibration command, which Task 11 repeats — it withholds **18 of the 28 authored files instead of 10**: `CHAP_Reiterkampf`, `COND_1`, `COND_2`, `COND_4`, `COND_5`, `COND_6`, `COND_7` and `DISADV_34` join the ten. Run rule-by-rule instead, `SA_40` and `SA_59` have **no** neighbours under any edge and withhold 1 file as before, and the other eight withhold **16** each. What leaves with them is every `scope: combat` modifier row in the corpus: the batch workspace is left with ten authored files carrying eight distinct non-reminder row shapes, none of them combat. **Nine of the ten golden rules therefore end with no worked precedent for any row shape they must produce** — every one but `SA_59`, which keeps `parameterOverride` — so the next measurement answers a harder question than the recorded run did (“can the pipeline encode this rule from its text with no worked example of the row shape in front of it”), and **no rule’s next number is comparable to its recorded verdict**: every rule sees eight fewer precedent files than the run that produced the 7/10, so a drop is the leak being removed rather than a regression. The arithmetic is pinned by `test_the_ten_rule_batch_withholds_exactly_the_documented_set`, its per-rule companion and `test_no_surviving_file_carries_a_combat_scoped_modifier_row`, because it is the number a human authorises ≈110 live model calls against and nothing asserted it before. Also corrected, in `docs/rules-pipeline-review-findings.md` §6 at source and in `docs/rules-pipeline-status.md` §1: §6 says “`SA_661`’s current Tier 1 pass may rest on this channel”, and that is wrong twice over — the recorded verdict is `false` with a `DISAGREE` in the run’s own summary table, not a pass, and the chapter file landed six commits *after* the run was recorded (`78802fb` after `505c721`), so the recorded 7/10 is not overstated by this channel. §6’s text and its ruling are left unchanged: only that sentence of evidence fails, the generalisation is sound, and building the mechanism found it a second instance (`SA_43`) that it did not know about

### Changed

- **`docs/plans/2026-09-20-rules-pipeline-and-authoring.md` is now a current-state document rather than an aspirational one.** Every task carries a Status line with what happened and the commit range where it landed, a status board sits at the top, and each acceptance criterion is ticked where met or annotated where it was amended or superseded. Two are called out rather than quietly ticked: Task 4's `SA_43` criterion is **superseded by the evidence** (there is no BE effect on that ability any more — the clause is the mounted-combat chapter page's, and the `mounted` condition survives on the `legality` row), and Task 6's "disagreement as a YAML comment" could not be met literally, because `lint.py` rejects every `#` comment while the same criterion demands a lint-clean file. Task 7's criteria are left unticked, because the gate failed. The Steps blocks are deliberately not ticked at all: several were amended by rulings during execution, so a step's text is a record of the plan and not a description of the code. Two plan defects fixed in passing — the `check.py`/`propose.py` invocations needed `-m`, which the Makefile targets already knew
- **ADR-0007 amended: provenance is the deterministic half's, and the cross-check has one blind spot.** The original Decision describes the propose step as an agent that proposes a "refreshed `checked`/`hash`"; that is withdrawn on the ADR's own logic, since a hash is a claim that a specific page was fetched and an agent cannot make it truthfully. An authoring agent never emits a `source:` block; the driver computes provenance with three outcomes and invents nothing, and a block that appears anyway is discarded **and reported**. The amendment also records the standing security position, which was a finding with no ADR home: the two-agent design detects *independent* error and has **no defence against a shared input**, so anything wrong with the rule text — stale prose, or injected instructions — steers both agents identically and the run reports `agree / ok / written`. Exfiltration risk low, integrity risk medium, and the mitigation is a prompt instruction rather than a mechanism
- **ADR-0008 amended: the fields the decision leaves open are registered, not free.** The ADR closes nine effect types and eleven predicates and says nothing about `actionEconomy.grants`/`forbids`, `legality.action`, the `gmFlag` slug and `parameterOverride.parameter`, which are open on purpose — an enum over 232 rules would make every novel ability a schema PR. Open is now recorded rather than unrecorded: each token is glossed in `specs/rules/vocabulary.yaml` and lint-enforced, so a new token is a one-line reviewable diff and the registry becomes the enum later. `parameterOverride.parameter` is the one open field with no registry and it has already cost a rule — a calibration proposal invented a parameter path that named no DSA constant, lints clean, and encodes to a silent no-op, past both the linter and the cross-check
- `DiceRollSheet` and `SkillCheckModal` now route all rolls through `DiceRoller` instead of calling `Int.random(in:)` inline
- Recorded talent stats moved from per-row tap-to-expand to a single section-level toggle, keeping rows uncluttered while still showing the theoretical % inline
- `scripts/build_rules_db/build_db.py`: `--effects` now takes the `specs/rules/` directory (one authored YAML file per rule) instead of a single hand-authored file; `RULES_EFFECTS` in the `Makefile` now defaults to `specs/rules`
- `specs/rules/schema.json`: `recovery`'s `attribute` is now required only when `operation: add` (a `scale`/`duration` recovery like `ADV_75` has no "stat recovered"); the `modifier.target` enum drops `duration`/`offHandPenalty`/`shieldPaBonus`/`anaesthesia`/`defense` — the exact legacy attributes the migration moved onto other effect types, so leaving them in `target` invited authoring `modifier target: shieldPaBonus` instead of the correct `parameterOverride`
- The ten golden-corpus rules (`SA_40`, `SA_41`, `SA_43`, `SA_48`, `SA_59`, `SA_62`, `SA_65`, `SA_66`, `SA_67`, `SA_661`) carry real provenance instead of the `UNVERIFIED` migration marker — a resolved rule-website URL, the Optolith `book`/`page`, today's `checked` date, and a `source.hash` computed with `scripts/rules_sync/normalise.py`, so `make rules-sync-check` reports them `ok` rather than `unverified`
- Encodings reviewed against the live rule text while the provenance was recorded: `SA_40`'s perception bonus moves from `scope: combat` to `scope: all` (it fires on a talent check, which no combat `CheckDomain` covers); `SA_48` gains an `aw` opponent row per Stufe (the rule penalises *defence*, which is PA or AW, and the schema has no combined target); `SA_59`'s two effects gain the `gmFlag: opponentUsesShield` precondition the rule states, plus a `reminder` for the shield-destruction outcome; `SA_65`'s `actionEconomy` changes from `forbids: attack` to `forbids: furtherActions` (the rule removes *all* remaining Aktionen, but not Reaktionen, so it is neither `attack` nor `allActions`); `SA_66` gains a `reminder` for the prone-legality clause; `SA_43` loses its `-1 BE` row and gains a `legality` effect for the mounted-command clause — that clause is the only one its *ability* page states, and the BE row belongs to the mounted context rather than to the ability (the stated reason was wrong on two of three counts; corrected under *Fixed* below, and the row is now authored as `CHAP_Reiterkampf`). Every effect now carries a `note` naming the clause it encodes
- `SA_62`'s `source.page` is 250, from the rule website, not the Optolith seed's 249 — ADR-0007's "the rule website wins and the local copy is the bug" is unqualified, and on this rule the seed is demonstrably pre-erratum (it is missing both cap clauses the live page carries), so a stale page number and a stale text are the same staleness. `book` is unaffected. The other nine agree with the seed exactly

### Removed

- `specs/data/rules.yaml` and `HARDCODED_EFFECTS` (plus the scraper's fallback path) in `scripts/scrape_effects/scrape_effects.py` — `specs/rules/` is now the single authored store for rule mechanics (ADR-0007)

### Fixed

- **`make rules-resolve` could effectively never fail** (task review, Important). Only a crawl problem whose text contained `": no index trail"` was treated as fatal, and that state is reachable only through `--groups` with an id the trail map does not have — which the Makefile never passes. A renamed category, a group index that would not fetch, a subtree truncated by the depth or page guard: each appended to a list, printed, and **exited 0**, with every rule behind it reported `unresolved` for a reason that had nothing to do with that rule. That is the exact shape of this task's own first live run, where one misread index page left 74 rules silently unresolved and only a human reading stdout noticed — the signal was there and nothing was listening. Crawl *problems* and resolution *results* are now different things: `CrawlResult.problems` holds `Problem(fatal, detail)`, fatal covers a category that could not be reached, a page that would not fetch, and either guard firing, while a page with no rule text and no anchors is **not** fatal because the site has several of those (Task 5 fix round 4 found five) and they simply yield no entry. Per-rule `unresolved`/`needs-review` stay non-fatal — they are legitimate results a reviewer clears one at a time, and 31 of them is a normal run. The map is still written on a fatal exit so the partial result is inspectable; the exit code is what says not to trust it, and `render_report` prints a `FATAL` marker plus a sentence saying the counts below it cannot be read at face value

- **Real book titles and page numbers in the resolver's docstrings and test cases** (task review, Minor, controller ruling). Neither rule prose nor an ability name, but the authored corpus deliberately stores a book as an opaque id (`book: US25001`) and these were the first real German book titles in git — one of them attached to `Seite 250`, which is a golden rule's page. Replaced with placeholders throughout, the same `Platzhalter-*` convention the fixtures already use. The self-audit that missed them had been written from recall; the corrected one greps the net diff of the whole task, which is the method, and it now reports no real book title, no real ability name outside a URL and no real page number anywhere in it

- **The documentation handed the authoring agents the calibration answer key** (whole-branch review, finding 1). `scripts/rules_sync/propose.py`'s `prepare_workspace` copies every file under `docs/adr/` into the workspace the two agents run in, and its redaction removes a withheld rule's id and German name and nothing else — so ten passages added in this branch's final documentation commits walked straight through it, stating graded rows in schema field names, a tier ladder as a parenthetical pair of values, an opponent-side value as a quoted German display string, and a `dice` row's recipient in English. Four of the ten rules the calibration gate grades were covered, including the one rule that has never passed in five runs. The consequence is not a low score but a meaningless one: the gate's own rubric reads a reproduction of the golden encoding as **evidence of contamination rather than success**, so the next run would have scored higher and measured less, and the improvement would have been credited to whichever input fix shipped alongside it. All ten passages are reworded in `docs/adr/0007`, `0008` and `0009` — **every decision and every rationale kept**, only the values from which one rule's encoding could be reconstructed removed, and each site now cites `specs/rules/<id>.yaml` (which the workspace withholds) or names the clause by position and mechanism, the convention the authored `note` fields already follow. Both edited ADRs carry a dated note recording the pass and the convention. `docs/rules-pipeline-status.md` §2's "two residual leaks are on record" is corrected: the two the Task 6 audit judged are still there and still accepted, and the ten that came later are gone. **`docs/adr/` is still copied into the workspace** — dropping it was considered and rejected, because `AGENTS.md`, `schema.json` and `vocabulary.yaml` stay in the workspace and can leak identically (the Task 6 round-2 leak was in `schema.json`), so removal narrows one surface, leaves three, and buys a false sense of closure; the property has to be checked, and now is

- **Verbatim German rule prose was committed, in files that assert it is not** (whole-branch review, finding 2). The Data Policy in `AGENTS.md` names documentation explicitly, and `propose.py`'s own `_find_text_leak` rejects a proposal in which three consecutive words of the source text reappear — while `CHANGELOG.md` carried a twelve-word clause of a rule page in quotation marks, `docs/rules-pipeline-status.md` carried the same one eleven lines below the sentence "No DSA rule prose appears here", and `docs/adr/0008` carried four shorter fragments. Each is reworded to name the clause by position and mechanism, which is how the authored `note` fields put it and carries the argument just as completely. Two sites the review did not list are fixed with them: this file's open-vocabulary entry, and `scripts/rules_sync/propose.py`'s `RuleInput.levels` comment, which quoted four words of seed text. `tests/rules/test_lint.py`'s prose-in-`forbids` fixture now uses a German sentence of our own invention rather than a real clause — the Data Policy covers fixtures too, and the test only needs prose. Nothing mechanical catches this class: the linter globs `specs/rules/` only, so the constraint over `docs/` and `CHANGELOG.md` is still enforced by authors alone (a grep-based check over both is filed, not built)

- **A hero imported before the combat-special-ability reclassification kept the old, narrower split forever, with no way back except re-import** (whole-branch review, finding 3). `OptolithImportService.isCombatSpecialAbility` moved from "the rule has a hand-authored effect with `scope: combat`" (9 matches) to "the rule's Optolith group is one of 3, 9, 10, 11, 12" (232 matches, see ADR-0008), but `combatSpecialAbilities`/`generalSpecialAbilities` are written once at import and never touched again — so the fix reached only heroes imported after it shipped, and two players at the same table with the same character file could get different numbers with nothing explaining why. **The user ruled: add a repair pass; re-import is explicitly not the migration path.** A new `SpecialAbilityClassificationRepair`, wired in beside `DerivedValueRepair.repairAll` with the same save-only-if-changed discipline, re-splits both arrays through the same predicate the import now uses — `CombatSpecialAbilityGroups.isCombat(groupId:)`, a new neutral home (`Hesindion/Engine/`, the same role `DerivedValueFormulas` plays for derived values, ADR-0006) so the import and the repair share one definition of "which groups are combat" rather than each carrying a copy of `[3, 9, 10, 11, 12]`. The group lookup is injectable (a `(String) -> Int?` closure, `RulesDatabase.shared.lookupGroupId` at the `ContentView` call site) so the repair's tests do not depend on the bundled `rules.db`. **What changes at the table for an existing hero:** any Sonderfertigkeit that moves from `generalSpecialAbilities` to `combatSpecialAbilities` starts working — concretely, a hero who owns `SA_884` (Plänkler-Formation, `Hero.hasPlaenklerFormation`) or `SA_160`/`SA_161` (the Trefferzonen Zonenaufschlag halving) had those abilities silently inert before this repair runs, and correct after; any other combat SA a hero already had classified correctly is unaffected, and the nine SAs the old predicate did match (`SA_40`, `41`, `43`, `48`, `59`, `65`, `66`, `67`, `661`) all stay classified as combat, so nothing that worked stops working. A trait with an id the database no longer recognises is moved out of `combatSpecialAbilities` into `generalSpecialAbilities` (the same "unknown means not combat" rule a fresh import applies) — a deliberate demotion, not a bug, and covered by its own test. **Fix round 1 (task review):** `repairAll` trusted `lookupGroupId` unconditionally, and `RulesDatabase.lookupGroupId` returns `nil` on every failure mode, not only "unknown id" — a broken or partially-built `rules.db` would have answered `nil` for every combat SA and demoted all of them, for every hero, in one silent save. `repairAll` now probes a fixed, always-known id (`SA_40`) before touching any hero and skips the whole pass if the probe itself comes back unknown, logging which case fired; it also now logs the moved rule ids, not only a hero count. **Also:** `ContentView`'s `.task` now constructs `RulesDatabase.shared` at launch (to get `lookupGroupId` for this repair), where it previously lazily constructed on first rule lookup — a missing bundled `rules.db` now `fatalError`s at launch instead of on first search, earlier and clearer, not a new failure mode

- **The four hand-synced `NON_RULE_FILES` copies had already drifted out of sync with each other's own cross-reference comments** (whole-branch review, finding 5). `scripts/rules_lint/lint.py`, `scripts/rules_sync/check.py` and `scripts/build_rules_db/verify_db.py` each carry a `NON_RULE_FILES` constant (plus `CHAPTER_PREFIX` in three of the four; `check.py` has no chapter-id concept), and `build_db.py` carried a fourth, lower-case, function-local copy nothing else could import. Each site's comment claimed to name the others it is kept in sync with by hand, and each was already wrong: `lint.py` didn't name `verify_db.py`, `verify_db.py` didn't name `check.py`, `check.py` didn't name `verify_db.py`. A non-rule file added to one and missed on the others is a silent skip nothing caught — `make rules-lint` and `make rules-db` would both pass while treating the new file inconsistently, and nothing in `tests/` asserted the four sets agreed. `build_db.py`'s copy is now a module-level `NON_RULE_FILES`, spelled and cased like the other three; every site's comment now names the other three and states the relationship precisely (`check.py`'s set is deliberately the other three plus `schema.json`, which the other three do not carry — preserved as a stated difference, not "fixed" into equality); and `tests/rules/test_shared_constants.py` imports all four `NON_RULE_FILES` constants plus the three `CHAPTER_PREFIX` spellings and asserts that relationship holds, so a fifth call site or a missed edit now fails the suite instead of drifting silently

- **A failed calibration gate read as a green test suite, and the Makefile command that starts an authoring wave carried no warning** (whole-branch review, finding 6). `python3 -m pytest tests/ -q` exited 0 and printed no verdict even though the calibration gate scored 7/10 and failed (`docs/rules-pipeline-status.md` §1, §3) — the only printed artefact was a neutrally-worded Tier 2 divergence report, and `rules-propose`'s Makefile recipe explained selectors and the disagreement protocol without ever saying a wave was blocked. `test_the_tier_1_result_is_the_fraction_the_gate_reported` now additionally prints `CALIBRATION GATE: FAILED — Tier 1 <n>/<total> — no authoring wave (docs/rules-pipeline-status.md §3)`, the fraction derived from the recorded run rather than hardcoded; the existing assertion, which pins a measurement rather than testing the pipeline, is unchanged, and the test still does not fail on the gate's own result (deliberate, `test_calibration.py:212-228`). The `rules-propose` recipe now prints the same warning, naming the status doc and the failed gate, before it does anything else — a plain `echo`, not an interactive prompt, so the target stays usable non-interactively

- **`main()` had no runner seam, so the workspace-sanitising call was reachable only by a live model run** (whole-branch review, finding 7). `scripts/rules_sync/propose.py`'s `main()` constructed `SubprocessRunner(...)` inline; `propose()` itself takes an injectable `Runner` and is thoroughly tested with `FakeRunner`, but `main()` could not be, because `tests/conftest.py`'s autouse guard forbids the real `claude` binary by design. That left `prepare_workspace(..., exclude_names=[r.name for r in rules])` covered by nothing but a live run: a refactor turning it into `exclude_names=[r.rule_id for r in rules]` — plausible, since `RuleInput` carries both fields and the parameter is named `exclude_names` — would pass every existing test and leave every withheld rule's German ability name unredacted across the workspace on the next live wave, which is precisely the contamination finding 1 fixed. `main()` now takes `runner_factory: Callable[..., Runner] = SubprocessRunner` and calls it instead of the class by name; `test_main_end_to_end_redacts_the_workspace_it_builds` (`tests/rules/test_propose.py`) drives `main()` end to end with a `FakeRunner` injected through that seam and asserts that neither a withheld rule's id nor its German ability name survives anywhere in the workspace `main()` actually built — it fails on exactly the `rule_id`-for-`name` substitution above (verified by temporarily making it and capturing the failure). `main()`'s closing summary line, which stated the workspace held "no authored file for, and no mention of" the run's rules as an achieved fact that nothing at that point re-verifies (the workspace is already torn down by the time it prints), is reworded to state what was requested of `prepare_workspace` instead of what was confirmed

- **The GS launch repair dropped any bonus out of `max`** (whole-branch review, finding 9). `DerivedValueRepair`'s Geschwindigkeit write preserved `bonus` but set `max` to the species base alone rather than `base + bonus` — a hero with `base 8, bonus 2, max 10` would have been rewritten to `max 8`, two Schritt lost, while the file's own `wundschwelle` write two lines above does this correctly (`max: ws.base + ws.bonus`). `max` is now `base + bonus` for Geschwindigkeit too, and the idempotence guard compares against that corrected `max` so a hero with a bonus is not rewritten on every launch. **This changes no number at the table today**: nothing writes a nonzero `geschwindigkeit.bonus` (the import always writes `bonus: 0`, and there is no other write site), so no stored hero currently has one for the repair to correct. `Ausweichen` and `Initiative` keep their existing, differently-conventioned `max` (the bare computed value, bonus not folded in) — pre-existing, out of scope for this fix, and now called out in a comment in the file so the inconsistency is a recorded decision rather than something the next reader has to re-derive

- **`RulesDatabase.lookupGroupId` could not distinguish a rule with no Optolith group from one in group 0** (whole-branch review, finding 10). `Int(sqlite3_column_int(stmt, 0))` returns `0` for a SQL NULL, same as a genuine group `0` — harmless today, since `0` was never in the combat-group set `isCombatSpecialAbility` checks, but a trap for a future `if lookupGroupId(id) != nil` caller, and finding 3's repair pass makes that caller more likely to be written. Now returns `nil` when `sqlite3_column_type` reports `SQLITE_NULL`. No caller's behaviour changes: the one caller (`OptolithImportService.isCombatSpecialAbility`, reused by `SpecialAbilityClassificationRepair`) already treated a missing group as "not combat", which is what `nil` and the old `0` both produced

- **The default `pytest` run made live network calls** (whole-branch review, finding 11). `tests/rules/test_normalise_live.py` fetches two live rule-website pages three times, with two one-second politeness sleeps, inside a module that plain `python3 -m pytest tests/ -q` collected unconditionally — the exact command `docs/rules-pipeline-status.md` §9 gives for a cold resume. It skips cleanly when the site is unreachable, but the documented test count was silently network-dependent and nothing let a caller deselect it. The module is now marked `@pytest.mark.live`; a new `pytest.ini` registers only that one marker (no `addopts`, no `testpaths`, no `filterwarnings`); and `docs/rules-pipeline-status.md` §9 now gives both counts and the deselect form — `python3 -m pytest tests/ -q -m "not live"` — alongside the full run

- **`scope: combat` had two disagreeing Swift readings, and nothing said so** (whole-branch review, finding 4). `Hesindion/Engine/SharedModifiers.swift`'s `mountedReliefDomains` and `Hesindion/Engine/RuleEffectModifiers.swift`'s `domainsForScope("combat")` are two independent Swift readings of the one authored token `specs/rules/CHAP_Reiterkampf.yaml`'s mounted-relief row carries, and they disagree — invisible only because `RuleEffectModifiers` still has no callers. Each site now carries a comment naming the other and stating which reading the authored row is actually implemented by today. `ModifierEngine.shared`'s registry (`ModifierEngine.swift:124-134`) now carries a comment stating the sharper hazard finding 4 raised: the hand-written definitions and the 109 authored effect rows encode the same mechanics, so wiring `RuleEffectModifiers.load` in means deleting the hand-written definition it replaces in the same commit, never appending to the list. ADR-0008 gains a dated amendment recording the same double-count hazard as a Consequence, plus — from the review's §3 "Ruling on item 3" — that the engine plan has **two** rows to reconcile against `CheckDomain`, not one: the mounted relief's row and the unmounted penalty's row (`SA_41`). **No Swift behaviour changes**: `domainsForScope` is deliberately untouched, because making the two readings agree is a behaviour change to a dead path and the engine plan's decision to make

- **The golden-corpus carve-out was documented wider than its own mechanism allows** (whole-branch review, finding 8). `tests/rules/golden/MANIFEST.yaml`'s description generalised the two safe root keys it was built for (`ruleset`, `source.title`) to "any rule-root key that is not an effect row", and `test_the_two_hashes_answer_different_questions` pinned that wider claim in one bundled assertion. `subgroup`, `excludes` and `id` also leave the `graded` hash unmoved, but are table-visible (maneuver-slot data, a Tier 2 mechanical field, and the rule's own identity) and still require a live re-run — an editor following the manifest's own remedy could refresh `bytes`, record a `golden_edits` entry and stop, without the calibration ever being re-run. `MANIFEST.yaml`'s description and `BYTES_REMEDY` (`test_golden.py`) now name the enumerated safe set (`note`, `source.checked`, `ruleset`, `source.title`) and its three non-safe near-misses explicitly; the bundled root-key mutation is now three individually-parametrized `test_a_safe_root_key_leaves_graded_unchanged` cases, each naming which key broke on a failure; a coupling test mutates `subgroup`/`excludes`/`id` independently, asserts each still leaves `graded` unmoved, and asserts each name is still quoted in both prose strings; and a further test extracts each prose string's own enumeration sentence and asserts it names *exactly* the four safe keys — catching not just a key going unmentioned but a near-miss being folded *into* the enumeration, which a presence check alone would miss. `TIER1_FIELDS` is untouched, per `docs/rules-pipeline-status.md` §8's standing ruling on the identical hazard for `note`

- **The vorteilhafte Position was gated on the Golgariten-Stil, and that was wrong in both directions.** `Hesindion/Engine/MeleeModifiers.swift` held two `+2 AT` definitions, `vorteilhaftePosition` and `golgariten`, both guarded by `ctx.hero.golgaritenActive(mounted:)` — which demands `SA_661` *plus* a Rabenschnabel *plus* a Großschild. Both source pages settle it against the app. `CHAP_Reiterkampf` clause 2 grants the advantageous position to **every** mounted hero fighting someone on foot; it is a chapter rule and owes nothing to any Sonderfertigkeit. `SA_661` clause 1 *raises* the AT ease that the advantageous position already confers, rather than granting one of its own — it raises an ease that already exists, so it cannot be the thing being raised. The arithmetic (`+2` baseline plus `+2` style = `+4`) was right all along; the gating was not. **Two numbers move at the table, in opposite directions, and this is deliberate:** a mounted hero *without* the style now gets the `+2` the chapter grants him and previously had to tick by hand, and a Golgarite mounted against *another rider* loses the `+4` he was being handed, because there is no advantageous position there for the style to raise. `ModifierContext` gains an `opponentOnFoot` GM flag — the `targetIsSurprised` pattern from ADR-0005, since the opponent is not modelled and its stance is the GM's call — which both `specs/rules/CHAP_Reiterkampf.yaml` and `specs/rules/SA_661.yaml` had already authored under exactly that name. The melee announcement screen shows it as a checkbox while mounted, defaulting on, because riding down foot fighters is the common case. The manual "Vorteilhafte Position" toggle still covers everything the engine cannot see, and now asks the engine whether it has already emitted the line, so it can never add a second `+2`.

- **Every hero got the human GS.** `OptolithImportService.swift:888` was `let geschwindigkeit = ResourceValue(base: 8, bonus: 0, max: 8)`, commented "GS = 8 (Mensch base)". GS is a species rule: the pinned Optolith source carries `mov` per race in `Data/univ/Races.yaml`, and **Zwerge are 6** while Menschen, Elfen and Halbelfen are 8 (`RaceVariants.yaml` overrides it for none of the ten variants, so those four are the whole of what the source knows). **Every dwarf hero has been two Schritt too fast.** The table now lives in `DerivedValueFormulas.geschwindigkeit(speciesId:)` beside the other formulas the import and the repair share, keyed on `PersonalData.speciesId` — the field ADR-0006 began persisting so a species-aware recompute would become possible. **Stored heroes are corrected at launch:** `DerivedValueRepair` gains GS, stays idempotent, and *skips* a hero whose species it cannot resolve rather than resetting them to the human value — `nil` is the normal state for anyone imported before `speciesId` existed, and assuming human would write the exact value being corrected. The import keeps the human fallback for an unrecognised species, because a hero must have a GS to display and that fallback is ADR-0006's already-recorded status quo. What changes at the table for a dwarf: the GS shown on hero detail (with `Hero.totalGsPenalty` — `belastungPenalty` plus the armour modifier — applied beside it), and the Flucht movement figures in `CombatDefenseViews` ("GS n Schritt" and "GS/2 = n Schritt"). `Hero.sturmangriffDamageBonus` is unaffected: it reads the *mount's* speed, not the hero's. GS still has no rule to cite — that is Task 12's derived-value work, and authoring it would have made this a wrong citation rather than a plausible constant
- **The chapter-id linter demanded ids the schema rejected.** `scripts/rules_lint/lint.py`'s `chapter_slug` kept digits and left the first character's case alone, while `specs/rules/schema.json`'s `#/$defs/ruleId` is `CHAP_[A-Z][A-Za-z]+` — so a page like `Regel_2.html` produced two contradictory errors at once ("id must be `CHAP_Regel2`" and "`CHAP_Regel2` does not match the pattern") with no spelling that satisfied both. The bug was in the slug function, not the pattern: letters-only is the whole of why the chapter and Optolith namespaces are disjoint **by construction** — every Optolith id ends in digits, every chapter id in letters — and widening it would demote that to a convention. Digits are now dropped from the derived slug and the first letter is upper-cased; a stem with no letters at all derives an empty slug, which is the existing "needs a real `source.url`" error rather than a legal `CHAP_2`. `lint.py` carries the reason the pattern must stay letters-only, and a test asserts every derived slug against the schema's own pattern so the two cannot drift apart again
- **`CHAP_Reiterkampf` repeated the `SA_43` mistake on the page that corrects it.** Clause 2 — a rider counts as being in an advantageous position against a foot fighter — was filed as an `UNENCODED:` reminder bundled with clause 1, on the reasoning that "both read off the mount, which is not a modelled entity". That is true of clause 1 (the mount's own initiative base) and false of clause 2 three ways: it reads off the *opponent's* stance, not the mount; `specs/rules/SA_661.yaml` already encodes exactly its shape (`modifier target: at, scope: combat, value: 2`, gated on `mounted` plus `gmFlag: opponentOnFoot`, both already in the grammar and the vocabulary); and the app has hardcoded it all along in `Hesindion/Engine/MeleeModifiers.swift:11-17`, rendered as "Vorteilhafte Position (+2)" by `CombatAttackViews.swift:404-443`. `SA_661`'s own note says it *raises an existing situational AT ease rather than adding an independent bonus* — the ease it raises is this clause, so until now the baseline sat unowned on an ability, which is precisely the `SA_43` error. The clause is now an encoded `modifier` row and the reminder covers clause 1 alone. The corpus therefore holds **three** of the page's constants, not two, and `CHANGELOG.md` and ADR-0008's amendment are corrected where they said two. **No Swift changed and no number moved at the table**
- Two chapter-rule notes stated reasons that were not true, which the `SA_43` round established is its own defect class even when the encoding is right. Clause 4–5's note said the app "models neither" weapon grip nor shield facing; it *does* model grip (`Hesindion/Views/CombatView.swift:114` `twoHandedGripActive`, `Hesindion/Views/CombatSetupViews.swift:489` `isTwoHandedOnly`), so the note now says which half is modelled and which is not. Clause 3's note asserted inexpressibility without ever mentioning `actionEconomy.forbids`, whose vocabulary is open by design; it now poses the design question it actually considered instead of announcing a conclusion. Clause 10's note carried the rule's own escalation constant, which is the wrong side of the schema's "locate the clause, never restate it" convention — reworded, here and in this file's entry above
- **Recorded, not fixed: the `vorteilhaftePosition` baseline is gated on the Golgariten style.** `Hesindion/Engine/MeleeModifiers.swift:15` guards the chapter-rule +2 with `ctx.hero.golgaritenActive(mounted:)`, which requires `SA_661` *plus* a Rabenschnabel *plus* a Großschild. So a mounted hero without that style gets no automatic +2 from a rule that binds every rider; the manual "Vorteilhafte Position" toggle in `CombatAttackViews` still works, so the number is reachable, just not automatic. This is the divergence Task 12 says to record and leave for a ruling — the authored row is `CHAP_Reiterkampf`'s and the Swift is the copy that disagrees, but correcting it changes numbers at the table and is the controller's call, not authoring's
- **The mounted Belastung relief applied to spell and liturgy casting, and should not have.** `Hesindion/Engine/SharedModifiers.swift`'s `encumbrance` reduced effective BE by 1 for every domain it covers whenever `mounted` was set, so a mounted caster in Plattenrüstung rolled a Zauber- or Liturgieprobe one point easier than the rules allow. The clause on <https://dsa.ulisses-regelwiki.de/Reiterkampf.html> eases *Kampfproben*, and the controller's ruling is that Kampf and Zaubern are neither the same nor related: **any non-combat action gets no reduction.** The relief is now scoped to the four combat domains (`meleeAttack`, `meleeParry`, `meleeDodge`, `rangedAttack`) via `SharedModifiers.mountedReliefDomains`, matching the `scope: combat` `CHAP_Reiterkampf` already authored. **The unmounted Belastung penalty is unchanged and still applies in all six domains** — it is only the mounted reduction that is combat-scoped, and `HesindionTests/MountedEncumbranceTests.swift` pins both halves plus the BE-1-eased-to-0 boundary. This changes numbers at the table for any mounted caster, in their disfavour, and it resolves the open question `CHAP_Reiterkampf` and this file recorded on 2026-09-21
- **`specs/rules/SA_43.yaml`'s note recorded a false reason for a correct removal.** It said the removed `-1 BE` effect had no clause on the ability's page, none in the Optolith seed and no Swift implementation. Two of the three are wrong: <https://dsa.ulisses-regelwiki.de/Reiterkampf.html> states the clause, and `Hesindion/Engine/SharedModifiers.swift:15` has implemented it all along. The removal itself stands, for a better reason — the effect is not that ability's, it is a mounted-context rule that binds any rider — and the note now says so and points at `CHAP_Reiterkampf`, which is where it now lives. `SA_43` is one of the ten golden files the authoring agents are calibrated against, so a wrong reason there is a lesson taught at scale. The manifest hash is updated; the calibration gate is not re-run, because Tier 1 grades effect rows with `note` excluded and nothing this edit touched is an input to any verdict — the argument is recorded under `golden_edits` in `tests/rules/calibration/2026-09-21-sonnet/RUN.yaml`, and `tests/rules/golden/MANIFEST.yaml` now states the carve-out and its one exception
- **`Hero.effectiveBE` reduced BE by 2 per Stufe of Belastungsgewöhnung (`SA_41`) instead of 1.** The rule's worked example (source page 246) states Plattenrüstung (BE 3) behaves as Kettenrüstung (BE 2) at Stufe I — a reduction of 1, not 2 — and `specs/rules/SA_41.yaml` already encoded the correct tiered values (−1 at Stufe I, −2 at Stufe II); the Swift formula (`totalEquippedBE - 2 * belastungsgewoehnungLevel`) was never brought in line with it and doubled the reduction at every Stufe (Stufe I: −2 instead of −1; Stufe II: −4 instead of −2). Every hero with Belastungsgewöhnung has been getting double the BE relief this whole time, which flows through `belastungPenalty` into AT, PA, AW, INI and GS — all five now come out one to two points higher (worse) than before for anyone actually encumbered by their armor's BE after the reduction
- **Combat special abilities were classified by whether someone had already hand-written an effect for them, not by the ruleset's own grouping.** `OptolithImportService.isCombatSpecialAbility` asked "does this rule already have a `scope: "combat"` effect?" — a circular check that misfiled every combat SF without authored effects (e.g. `SA_884` Plänkler-Formation, `SA_661` Golgariten-Stil) under `generalSpecialAbilities`, where no combat code looks. Classification now uses `rules.group_id IN (3, 9, 10, 11, 12)` (Kampf, Kampfstile bewaffnet/unbewaffnet, Kampf erweitert, Befehle) via the new `RulesDatabase.lookupGroupId(_:)`. See ADR-0008
- `git rm --cached Hesindion/Resources/rules.db` — the file has been listed in `.gitignore` since it was added but was already tracked, so the ignore rule never applied and the app kept shipping a stale, committed database
- **Wundschwelle, Ausweichen and Initiative rounded down instead of up.** DSA 5 rounds derived values up — the rule website's own example is KO 11 → Wundschwelle 6, which the app computed as 5. Every hero with an odd KO had a Wundschwelle one point too low; odd GE cost a point of Ausweichen and odd MU+GE a point of Initiative. Ausweichen and Initiative affect defence and turn order, so this is a visible change at the table. Existing heroes are corrected automatically at next launch
- The Wundschwelle modifiers Eisern (`ADV_54`, +1) and Gläsern (`DISADV_56`, −1) were never applied — the bonus was hardcoded to 0, unlike the neighbouring Seelenkraft and Zähigkeit traits
- Trefferzonen: the Fokus-Regeln activation was stored in the combat-session block and wiped by "Kampf beenden", so a group's house rules had to be re-enabled at the start of every fight. Activation is now a persistent per-hero setting on the hero settings screen and is no longer part of the combat flow
- Trefferzonen: a hero without a Selbstbeherrschung talent row had the Wundeffekt applied automatically, with no probe offered. Selbstbeherrschung is a DSA 5 basic ability every hero has, so that case was never a rules outcome — the probe is now always offered (falling back to Fertigkeitswert 0 if the row is missing), and a wound effect is applied only on a rolled, failed probe. Not rolling now means "the GM has not adjudicated" and applies nothing, removing the asymmetry where declining to roll was better than rolling
- Trefferzonen: the Arme Wundeffekt's "Waffe ablegen" button cleared the hero's equipped weapon immediately instead of participating in the confirm transaction, so navigating away from an unconfirmed take-damage flow left the hero disarmed with no LP change and no log entry. The action is now staged and only applied on confirm, alongside the single LP write and the log entry
- `make test`/`test-ui`/`test-ui-record` no longer clone the simulator per test worker (`-parallel-testing-enabled NO`, `-maximum-concurrent-test-simulator-destinations 1`); `test-ui-record` uses the correct `SNAPSHOT_TESTING_RECORD=all` value
- **`SA_41`/`ADV_25`/`ADV_44` lost their Stufe ladder in the rules migration.** All three are leveled (`levels` 2/7/3) but were authored as one flat effect, so `RuleEffectModifiers`'s exact-tier match fired that single value at *every* owned Stufe (`SA_41` gave −2 BE at Stufe I instead of −1; `ADV_25` gave +1 LE at Stufe VII instead of +7; `ADV_44` gave +1 LE/cycle at Stufe III instead of +3). The migration had in fact kept the wrong one of the two duplicate legacy sources — `HARDCODED_EFFECTS` held `SA_41` correctly as two tiered rows before it was deleted. All three are now authored as full tier ladders
- `scripts/build_rules_db/build_db.py`: the `effects.attribute`/`.value` display columns now fall back across each effect type's own field (`state`, `parameter`, `forbids`, `grants`, `recipient`, `add`, `level`, `action`) instead of reading only `target`/`value`, so the live `RuleDetailView` no longer renders a `parameterOverride`, `actionEconomy`, `dice` or `stateGain` row as a bare, empty type token (e.g. `SA_59`, `SA_65`, `SA_67`)
- `specs/rules/SA_661.yaml`: the "mounted vs. foot fighter" AT bonus was encoded as one opaque `gmFlag`, discarding the half of the condition (`mounted`) the engine already checks mechanically elsewhere in the same file. `when` now ANDs `{mounted: true}` with a narrower `{gmFlag: opponentOnFoot}`
- `scripts/rules_lint/lint.py` printed "27 rule file(s)" while linting 26 — the file count included `SOURCES.yaml`, which is deliberately excluded from linting
- `scripts/scrape_effects/scrape_effects.py` silently dropped rules it couldn't scrape and couldn't fall back for; it now collects every missing rule id and prints them unconditionally at the end, per ADR-0007's "a missing rule must be visibly missing"
- **`scripts/rules_sync/normalise.py` hashed a page with an empty content container as the SHA-256 of the empty string.** It failed loudly when `#main`/`<main>` was *missing*, but a container that exists and is empty normalised to `""` — and five real rule-website pages are exactly that (`sf_kampfsonderfertigkeiten.html`, `Best_Tiere.html`, `ruestkammer.html`, `RS_Waffen.html`, `RS_Ruestung.html`, whose content is rendered client-side or sits outside `#main`), all verified live to produce the identical hash `sha256:e3b0c442…b855`. Recorded as a rule's `source.hash`, that would have compared `ok` forever while verifying nothing. An empty or whitespace-only extraction (including a container holding only stripped elements such as `<script>` or the CAPTCHA widget) now raises `ContentContainerEmpty`, a sibling of `ContentContainerNotFound` under the new `ContentContainerError` base, and `make rules-sync-check` reports it as `structure-changed` and exits non-zero, naming the URL and distinguishing "no content container" (the site's markup changed) from "empty content container" (the rule text is not in the fetched HTML at all)
- **Whole-session review fix wave (2026-09-22).** `docs/adr/0007`, `0008`, `0009` and `AGENTS.md` are copied into the sanitised authoring workspace, where redaction removes only a withheld rule's id and its German ability name. Six passages across them stated a graded row anyway — as a Swift symbol whose name *is* the field, as a domain count, or as plain English naming what a row does — one of them a *negative* statement, which Tier 1 grades as surely as a positive one, about the rule the stability measurement most depends on. All six now name the clause by position and cite `specs/rules/<id>.yaml` or a Swift `file:line`. `tests/rules/test_workspace_leaks.py` gains a fifth probe for the symbol-name shape, and its docstring names the four targets that probe deliberately does not cover
- **ADR-0008's `CheckDomain` amendment sent the next engineer to the wrong line.** It described the second domain-set disagreement as being over the *same* authored token as the first (it is a second token at a second site), and read one of the two rows "through `RuleEffectModifiers.domainsForScope`" while assigning the other row to the site that in fact implements both — `domainsForScope` is reached only from `RuleEffectModifiers.load`, which nothing calls, so no row is read through it at all. Corrected against the Swift, still without naming either row's fields. One residual in the same ADR stays accepted and is re-recorded dated, on today's graph-closure facts rather than on the pre-closure ones it was first accepted under
- **The documented cold-start order could not run.** `scripts/rules_sync/resolve.py` hard-fails when `rules.db` is absent, so `AGENTS.md` and `docs/rules-pipeline-status.md` §9 — which both listed `make rules-resolve` before `make rules-db` — aborted at step 2 on a genuinely fresh clone, leaving unarmed the two guards `AGENTS.md` exists to arm. Both now read `rules-db → rules-resolve → rules-db → pytest`, matching the `Makefile`
- **`scripts/build_rules_db/verify_db.py`'s `UNVERIFIED_URL` was the third copy of a hand-synced constant and the only one no test imported**, while its own comment claimed the test that would have caught a drift. Drift there either reports every file still holding the placeholder as having dropped its `source_url`, or treats the placeholder as a reviewed URL and silently excludes those rules from `check_resolution_reached_the_db`. `tests/rules/test_shared_constants.py` now asserts all three spellings
- `verify_db.py`'s failed-check count included `check_resolution_reached_the_db`'s own trailing summary line, so one real failure reported as "2 check(s) failed"
- `tests/rules/test_golden.py`'s `_SYNTHETIC_RULE` carried `SA_65`'s id and that file's first graded row verbatim, under a comment saying it "can never be mistaken for one of the golden ten". It is now genuinely synthetic
- `tests/rules/test_propose.py`'s section-6 test cited a rationale later commits in the same span retracted. The test is correct and unchanged; only its stated reason is
- `scripts/rules_sync/resolve.py`'s module docstring mixed two denominators for the resolver's scope, which is the 232 rules in combat groups 3, 9, 10, 11 and 12
- `Hesindion/Services/SpecialAbilityClassificationRepair.swift`'s sanity-probe comment said the probe turns a lookup failure into "do nothing". That is true of a *total* failure and false of a partial one: a `rules.db` carrying the probed group-3 row but not the group 9/10/11/12 rows answers the probe and then demotes every Kampfstil SA, for every hero, in one save. The comment is corrected; the guard's behaviour is deliberately unchanged

### Changed

- Schmerz now flows through the new states system rather than a standalone computation, with Belastung counting toward the −5 Zustand cap
- The Beengte-Umgebung combat toggle now persists as the `eingeengt` status (its single source of truth) and survives combat exit

## [0.3.0] - 2026-03-28

### Added

- General-purpose dice roller ("Würfeln") command with configurable count and sides, tumble animation, and action log integration

## [0.2.0] - 2026-03-23

### Added

- Generic ModifierEngine for unified modifier calculation across melee, ranged, defense, magic, liturgy, and talent checks
- Magic casting flow — standalone SpellProbeModal with expandable modifications section
- Combat spell casting — "Zaubern" action with spell selection, setup, multi-round casting with round tracker, and 3d20 execution
- Magic & Karma section in hero detail showing spells, liturgies, cantrips, and blessings with swipe-to-roll
- SkillCheckModal — unified 3d20 skill check UI shared by talents and spells
- Magic-specific modifiers: maintained spells, foreign tradition, gestures/formula, Bann des Eisens, distraction, spell modifications
- Effects scraper for populating rule effects from ulisses-regelwiki.de
- DB-sourced rule effect modifiers via RuleEffectModifiers loader

### Changed

- Melee attack, defense, and ranged modifiers now use ModifierEngine instead of hardcoded logic
- TalentProbeModal refactored to delegate to generic SkillCheckModal
- CheckDomain split: meleeParry and meleeDodge replace single meleeDefense for cleaner modifier targeting
- RulesDatabase.lookupEffects() made internal for engine access
- build_db.py now accepts both dict and flat list YAML formats for effects import

### Added (prior)

- Abenteuer (Adventure) system with weather generation
- Aventurian calendar with 12 months + Namenlose Tage
- Weather generator porting DSA 4.1 WdE p.156ff tables
- 13 climate regions from Ewiges Eis to Südmeer
- Day-by-day and bulk weather generation
- Date jumping with time-jump markers in timeline
- Plain-text weather export via share sheet
- Hero ↔ Adventure linking via hero settings
- New sidebar section for adventures

- Fernkampf (ranged) execution view: W20 FK roll with animated dice, modifier breakdown, critical/fumble confirmation, Schip reroll, and distance-adjusted damage formula
- Fernkampf criticals and fumbles mirror melee: roll 1 confirms critical hit (halved defense + double damage), roll 20 confirms fumble using the dedicated FK fumble table
- Opponent defense view now shows "Keine Parade mit Waffe möglich" and defense penalty hint for ranged attacks
- `CombatAction.fernkampf` added so FK fumbles route to the correct `FumbleTableType.fernkampf` table

- Passierschlag (free strike) view: AT-4 attack with no maneuvers, no critical successes or fumbles, with animated dice rolling and damage calculation
- Passierschlag button on critical parry success in defense outcome
- Per-profession color schemes for hero detail views (19 palettes: priests by deity, warriors, mages, mundane)
- Hero settings view accessible via command palette ("Einstellungen für <Hero>")
- Color scheme picker with visual swatch previews and automatic profession-based detection
- Combat execution rolls (AT/PA/AW) now logged automatically with outcome and effective value
- Schip reroll usage logged as dedicated combat action entry

### Changed

- Combat log descriptions enriched: critical/fumble markers on attacks, TP instead of "Schaden ausgeteilt", structured schip/fumble/flucht/opponent-defense text

- Mount pre-check (Galopp + Reiten) redesigned as single-screen vertical flowchart with collapsing steps and connector arrow
- Talent probe modal: enlarged modifier buttons (44pt tap targets) for easier use
- Talent probe modal: constrained max width to 400pt on wide screens
- Sidebar title centered via toolbar principal item
- Panel toggle buttons now have filled backgrounds with white icons (no borders)
- Redesigned landscape sidebar panel buttons — bold 48×48 squares flush to screen edge with distinct amber/teal/purple colors, dark mode adaptive

### Fixed

- LP (Lebenspunkte) calculation now includes species base value (e.g., +5 for humans, +8 for dwarves) — previously only used KO × 2
- "Held importieren" button text readability (black text on gold background)
- Selected hero row visibility in sidebar (increased highlight opacity)
- Removed unnecessary trailing border from attributes column

### Added

- Action log (Protokoll) with event sourcing — all talent checks, combat damage, healing, and resting are recorded as reversible log entries
- Log panel (Protokoll) viewable in split-screen with combat grouping, swipe-to-delete with automatic state reversal
- Split-screen layout — Notes, Protokoll, and Regelwerk panels available in 50/50 split (landscape) or full-screen overlay (portrait)
- Heilung command — heal hero with source tracking and logging
- Reittier: Heilung command — heal mount with logging
- SchemaV3 migration with LogEntry model
- Adaptive content width modifier for iPad: proportional margins (~6% per side) with 700pt max-width cap, standard 16pt padding on iPhone
- Notes panel ("Notizen") sidebar toggleable via toolbar button on iPad in both hero detail and combat views
- Hero.notes property persisted via SwiftData with SchemaV2 lightweight migration
- ContentWithNotesLayout wrapper for consistent notes panel integration across views
- Adaptive attributes column fixed to left side in iPad landscape mode
- Inline probe attribute abbreviations (e.g., KL, CH, GE) in talent rows
- Personal data fields display in responsive grid layout (2-3 columns)
- Ctrl+K keyboard shortcut to open command palette
- LP (Lebenspunkte) bar for all pets in hero detail view
- Mount LP bar in combat view when mounted combat is active
- Mount damage with automatic Reiten (Kampfmanöver) check — penalty scales +1 per 5 SP; Sturz warning on failure
- "Reittier: Schaden" command in command palette for normal mode
- TalentProbeModal now accepts an initial modifier for pre-applied penalties

### Changed

- Replaced notes-only right sidebar (ContentWithNotesLayout) with flexible SplitContentLayout supporting Notes, Protokoll, and Regelwerk panels
- Panel toggle buttons now built into layout instead of toolbar
- Combat view: replaced per-element horizontal padding with adaptive content width modifier for consistent iPad margins
- Mount combat: Reiten check now uses the full talent probe modal with dice rolls instead of a simple Yes/No dialog
- Moved mount attacks from combat root view to attack selection screen
- "Ausruestung wechseln" button restyled with teal accent for better visual distinction
- Weapon and shield selection merged into single loadout step
- Renamed project from iDSACompanion to Hesindion (after Hesinde, DSA goddess of wisdom)

### Added

- Niederreiten and Sturmangriff zu Pferd as selectable attacks in the attack choice screen
- Galopp confirmation and Reiten (Kampfmanöver) check flow before mounted charge attacks
- Mount attacks (regular, Niederreiten, Sturmangriff zu Pferd) grouped in attack choice view alongside hero attacks
- Mächtiger Schlag reminder for mount attacks: when the mount has "Mächtiger Schlag" in its special skills, an info banner shows during attack execution explaining the Kraftakt check rule, including the calculated penalty from the mount's KK
- SwiftData VersionedSchema and SchemaMigrationPlan for safe schema migrations
- Modifier breakdown for defense actions: Parieren and Ausweichen now compute and display labeled modifier lines (Belastung, Schmerz, Golgariten-Stil PA bonus, Plänkler-Formation AW bonus, mounted dodge penalty, dual-attack penalty) with an effective total, matching the attack announcement breakdown
- Vorstoß defense lock: Parieren and Ausweichen buttons disabled (grey) when Vorstoß active this round, with warning note below
- Schmerz indicator in combat root view: badge showing pain level and penalty when effectiveSchmerzLevel > 0
- Mount attacks section in combat root: when mounted, show pet attack list with AT values; each attack skips the announcement step
- Two-handed weapon selection disabled when mounted (greyed out with "Beritten" note in loadout view)
- Auto-select mount INI base on initiative screen when mounted mode is active
- Modifier breakdown in combat execution view: labeled rows show base AT/PA/AW, each situational modifier (Belastung, Schmerz, Vorteilhafte Position, maneuver, dual-wield, off-hand) and manual zusätzlich adjustment, with a dark "Effektiv" total bar; falls back to simple value box for defense paths without a full breakdown
- Announcement step between weapon selection and execution: maneuver selection (Normal, Finte, Wuchtschlag, Vorstoß, Schildspalter, Sturmangriff) with Vorteilhafte Position toggle and full AT/damage modifier pre-calculation
- Combat setup step between armor selection and initiative: Plänkler-Formation toggle (AT or AW bonus) and mounted toggle for eligible heroes
- Vorstoß and active maneuver state reset on round advance
- Schmerz penalty warning and modifier applied to talent probe results in TalentProbeModal
- Aufmerksamkeit (SA_40) contextual hint shown in talent probe for Sinnenschärfe (TAL_8)
- Schmerz (pain) tracking: raw level from LP thresholds, Zäher Hund (ADV_49) reduction, penalty computation
- Combat ability detection helpers: Aufmerksamkeit, Golgariten-Stil, Berittener Kampf, Finte, Wuchtschlag, Vorstoß, Schildspalter, Plänkler-Formation
- Mount detection and Sturmangriff damage bonus computation
- Combat setup screen flag (needsCombatSetup) for Plänkler-Formation and mounted heroes
- Combined equipment loadout view (weapons + shields in one screen with checkboxes)
- Dual-wielding support for heroes with Beidhaendig (ADV_5) advantage
- Pre-attack choice: "Eine Waffe" vs "Beide Waffen" for dual-wield heroes
- Two-handed grip option (+1 TP, -1 PA) for eligible one-handed weapons
- Vorteilhafte Position per-roll toggle (+2 AT/PA/AW) on combat execution screen
- Off-hand penalty display and calculation for dual-wield combat
- Dual-attack penalty tracking per combat round (resets on round advance)
- Dual-attack second strike flow with fumble handling (Patzer cancels second attack)
- Combat loadout system: select main weapon + shield at combat start, persists across sessions
- Passive shield PA bonus on main weapon parade (single modifier per DSA 5 rules)
- Active shield parry with doubled PA bonus
- Shield-specific combat notes (e.g., Großschild "+1 PA vs. Fernkampf")
- Loadout-aware Angriff/Parieren: skip weapon list when no shield, simplified choice when shield equipped
- "Ausrüstung wechseln" button to change loadout mid-combat
- Armor equip/unequip system with `isEquipped` toggle (swipe-left in hero detail, toggle in combat)
- Belastung (encumbrance) system: effective BE, Belastungsgewöhnung support, penalties on AT/PA/AW/INI/GS
- Belastung penalty display as separate modifiers on combat stats (e.g., "AT 12 (-1)")
- Combat flow: armor selection → initiative roll → combat root (replaces direct-to-root)
- "Schaden nehmen" combat action: TP input → RS calculation → LP reduction with confirm
- Armor management during combat via shield button and modal sheet
- INI/GS direct modifiers on armor model (parsed from Optolith `iniMod`/`movMod`)
- 17 new DE/EN localization strings for damage and armor system
- Adaptive border colors and combat technique AT/PA design docs
- Ranged weapons UI with combat technique ID-to-name resolution
- RangedWeapon model and ranged weapons import
- Attribute-by-ID resolver to Attributes model
- Combat technique detail lookup in RulesDatabase
- Full set of 59 standard talents included on import (missing ones default)
- Hero avatar display in sidebar list rows
- Direct Optolith export import (replacing custom hero JSON import)
- Rule lookup sheet, app icon variants, and UI polish
- Rules system with spells, liturgies, and rulebook UI
- Regenerieren command to restore Lebensenergie via 1W6 roll
- Combat section labels, step transitions, and expanded combat spec

### Fixed

- PA rounding: use ceil(KtW/2) instead of floor per DSA 5 rules
- Initiative re-roll sheet now includes Belastung penalty in base INI
- Weapon AT/PA calculations to use DSA 5 formulas
- AT/PA calculation and listing of all combat techniques
- Numeric select-option IDs resolved to names during hero import
- Button heights in combat root view
- LP bar display at zero value

### Changed

- Parieren/Ausweichen moved to own rows
- LP bar pattern reused in hero view Lebensenergie modal
- INI row height reduced by halving vertical padding
- Added Makefile, fixed UIFileSharingEnabled, and simplified command palette
